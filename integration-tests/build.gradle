plugins {
    id 'base'
}

apply from: '../versions.gradle'
apply from: '../common.gradle'

subprojects {
    repositories {
        maven {
            url = "../../build/sdk/repo"
        }
        mavenCentral()
    }
}
