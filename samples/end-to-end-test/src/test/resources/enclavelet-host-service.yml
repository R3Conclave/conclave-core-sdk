apiVersion: v1
kind: Service
metadata:
  name: enclavelet-host
spec:
  type: NodePort
  ports:
  - port: 8080
    nodePort: 30080
    protocol: TCP
    name: http
  selector:
    app: enclavelet-host
